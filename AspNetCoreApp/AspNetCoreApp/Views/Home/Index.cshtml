@model CatListViewModel
<h1>Home Page</h1>

<ul>
    @foreach (var cat in Model.Cats)
    {
        <li>@cat.Name</li>
    }
</ul>
<div>test pull request from visual studio</div>
<div page-model="@Model.PagingInfo" page-action="Index"></div>